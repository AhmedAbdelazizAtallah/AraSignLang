# Fonts
Place an Arabic-capable TrueType font here named **`arabic.ttf`** (e.g. Google
**Cairo**). Used by Pillow for baked-in overlays and PDF reports. If missing,
the web UI still renders Arabic via CDN.
